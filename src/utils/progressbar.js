/** @format */

// DEPRECATED: Import { progressbar } from "./playerUtils" instead.
// This shim exists for backward compatibility with unrewritten commands.
const { progressbar } = require("./playerUtils");
module.exports = { progressbar };